# AGENTS

See [CLAUDE.md](./CLAUDE.md) for project overview, commands, architecture, and code conventions.

Per-directory notes live in the `AGENTS.md` file inside each directory (`app/`, `components/`, `hooks/`, `lib/`, `scripts/`).
